    <!-- category select -->
    <div class="category-select-box">
	  <label>选择类别:</label>&nbsp;
      <%= select("category", "id", @categories.collect {|c| [ c.name, c.id ] }, {:include_blank => '全部'}) %>
	</div>
	<!-- pagination -->
  <div class="pagination-box-top ideas-pagination">
    <%= will_paginate @ideas,:param_name => :ideas_page %><div class="clear"></div>
    </div>
  <!-- begin of ideas -->
  <div class="ideas-all" id="ideas">
    <%= render @ideas %>
  </div>	
  <!-- end of ideas -->
    <!-- pagination -->
  <div class="pagination-box-bottom ideas_pagination">
    <%= will_paginate @ideas,:param_name => :ideas_page  %><div class="clear"></div>
  </div>
